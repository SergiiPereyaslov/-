# Етап 3. Дизайн-система

Реалізація — Tailwind CSS v4 (CSS-first, `@theme` у `web/src/app/globals.css`).
Документ описує рішення; джерело істини — код.

## 1. Позиціювання в кольорі

Ніша перенасичена «зеленим еко». Щоб не зливатися, будуємо палітру на
**крафтовому папері як матеріалі**, а зелений лишаємо тільки для дії:
крафт — це фон і фактура, зелений — це кнопка.

| Роль | Токен | Light | Dark |
|---|---|---|---|
| Основний текст | `--color-ink` | `#1A1D1A` | `#ECEFEA` |
| Приглушений текст | `--color-muted` | `#5F6660` | `#A3AAA4` |
| Тло сторінки | `--color-bg` | `#FBF9F5` | `#131614` |
| Тло картки | `--color-surface` | `#FFFFFF` | `#1B1F1C` |
| Крафтове тло секції | `--color-kraft` | `#F2E8D8` | `#242822` |
| Рамка | `--color-border` | `#E3DDD1` | `#2E332E` |
| Дія (CTA) | `--color-primary` | `#0E7A4B` | `#2FA46F` |
| Дія: наведення | `--color-primary-hover` | `#0A6640` | `#3CB77E` |
| Акцент (терміновість, знижка) | `--color-accent` | `#C8721B` | `#E08B33` |
| Успіх | `--color-success` | `#15803D` | `#4ADE80` |
| Помилка | `--color-danger` | `#B42318` | `#F87171` |

Правило: **зелений тільки на кнопках дії та статусі «в наявності»**.
Помаранчевий акцент — тільки на «−12%», «залишилось», «24 год». Якщо зелений
з'явився у фоні секції — це помилка.

Обидві теми обов'язкові: `prefers-color-scheme` + перемикач.

## 2. Типографіка

| Роль | Шрифт | Розмір | Насиченість |
|---|---|---|---|
| H1 | Manrope | 32 / 44 px (моб/деск) | 700 |
| H2 | Manrope | 24 / 32 | 700 |
| H3 | Manrope | 19 / 22 | 600 |
| Тіло | Inter | 16 | 400 |
| Дрібне | Inter | 14 | 400 |
| Ціна велика | Manrope | 24 / 28 | 700, табличні цифри |
| Артикул, ТТХ | Inter | 13 | 400, `tabular-nums` |

Обидва шрифти — `next/font/google` з підмножинами `latin` + `cyrillic`,
`display: swap`, самохостинг. Кирилиця обов'язкова: без неї український
текст падає на системний шрифт і псує макет.

Цифри всюди, де є ціни й розміри, — `font-variant-numeric: tabular-nums`,
щоб колонки цін не «стрибали».

## 3. Сітка та простір

- Контейнер: 1280 px, бічні відступи 16 (моб) / 24 (планшет) / 32 (деск)
- Сітка товарів: 2 колонки (моб) → 3 (768) → 4 (1280)
- Крок відступів: 4 px. Використовувані значення: 4, 8, 12, 16, 24, 32, 48, 64, 96
- Вертикальний ритм секцій: 48 (моб) / 80 (деск)

## 4. Радіуси, тіні, межі

| Токен | Значення | Де |
|---|---|---|
| `--radius-sm` | 8px | інпути, чіпи |
| `--radius-md` | 12px | картки, кнопки |
| `--radius-lg` | 20px | модалки, hero-блоки |
| `--shadow-card` | `0 1px 2px rgb(0 0 0 / .04), 0 4px 12px rgb(0 0 0 / .05)` | картка товару при наведенні |

Тіні мінімальні. Розділення — рамкою `--color-border`, не тінню: на крафтовому
фоні тіні виглядають брудно.

## 5. Компоненти

| Компонент | Стани | Примітки |
|---|---|---|
| Button | default / hover / active / focus-visible / disabled / loading | 3 варіанти: primary, secondary (рамка), ghost |
| Input, Select | default / focus / error / disabled | помилка — текст під полем, не тільки колір |
| ProductCard | default / hover / у кошику | степер кількості просто в картці |
| QtyStepper | −/значення/+ | мінімум 1, ручне введення |
| FilterGroup | згорнутий / розгорнутий / активний | лічильник обраних |
| Chip | обраний фільтр з ✕ | |
| Badge | «−12%», «хіт», «новинка», «є логотип» | акцентний колір лише для знижки |
| Breadcrumbs | | з мікророзміткою |
| Accordion | FAQ, характеристики, мобільне меню | |
| Sheet | мобільні фільтри, кошик | пастка фокуса, Esc |
| Modal | «Отримати прайс» | |
| Toast | «Додано в кошик» | автозникнення 4 с |
| EmptyState | порожній кошик, немає результатів | завжди з дією-виходом |

## 6. Фокус і доступність

- `:focus-visible` — обведення 2 px `--color-primary` з відступом 2 px.
  Ніколи не `outline: none` без заміни.
- Мінімальна площа дотику 44×44 px.
- Контраст перевіряється для обох тем; `--color-muted` на `--color-bg`
  дає 5.2:1 у світлій і 6.1:1 у темній.
- Анімації вимикаються за `prefers-reduced-motion`.

## 7. Зображення

- Формати AVIF → WebP → JPEG через `next/image`
- Співвідношення сторін товару 1:1, фон нейтральний
- `sizes` прописується для кожної сітки, щоб не тягнути 1200 px у мобільну картку
- Перше фото товару в категорії — `priority` тільки для першого екрана
- Обов'язковий `alt` за шаблоном: «{назва} {об'єм} {колір} — SmartEcoPack»

## 8. Бюджет продуктивності

| Метрика | Ціль | Факт (2026-09-07) |
|---|---|---|
| LCP | < 2.0 с | міряти на бойовому хостингу |
| CLS | < 0.05 | міряти на бойовому хостингу |
| INP | < 200 мс | міряти на бойовому хостингу |
| JS на сторінці категорії | < 120 KB gzip | **~180 KB — не виконано** |
| CSS | — | 7.5 KB gzip |
| HTML сторінки категорії | — | 17.7 KB gzip |
| Шрифти | 2 сімейства × 2 накреслення | 4 файли woff2 (latin + cyrillic) |

**Про невиконаний бюджет JS.** 120 KB був нереалістичною цілью для
React-застосунку: ~175 KB з виміряних 180 — це базовий рантайм React 19 і
Next 16, власний код сторінки додає одиниці кілобайтів. Скоротити можна лише
прибравши інтерактивність із картки товару в сітці (степер і «в кошик»),
що підніме кількість кліків до замовлення — не рекомендую.

Пом'якшує ситуацію те, що всі 386 сторінок віддаються статичним HTML: контент
видно до гідратації, тому LCP від обсягу JS практично не залежить. Це і є
основний важіль швидкості та головна причина вибору Next.js замість готової
платформи.
