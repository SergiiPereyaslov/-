# Етапи 8–10. Стан робіт, чек-лист запуску, що потрібно від клієнта

Дата: 2026-09-07

## 1. Що зроблено

| Етап | Стан | Де |
|---|---|---|
| 1. Семантика, структура, мапа 301 | ✅ | `01-semantyka-ta-struktura.md`, `redirects-301.csv` |
| 2. UX-прототип | ✅ | `02-ux-prototyp.md` |
| 3. Дизайн-система | ✅ | `03-dyzayn-systema.md` + `web/src/app/globals.css` |
| 4–5. Фронтенд, каталог, фільтри, кошик | ✅ | `web/` |
| 6. Адмінка / CMS | ✅ | каталог, заявки, категорії, блог, імпорт CSV |
| 7. Контент і міграція | 🟡 частково | мапа 301 побудована на краулі; реальних товарів досі немає |
| 8. SEO-налаштування | ✅ | розмітка, sitemap, robots, canonical, hreflang, 301 |
| 9. QA | ✅ | тести в Chromium, див. §3 |
| 10. Запуск | ⛔ блокований даними клієнта | чек-лист у §4 |
| 11. Двомовність uk/ru | ✅ | два дерева маршрутів + словники |
| 12. Каталог за структурою клієнта | ✅ | `05-katalog-analiz.md`, 6 груп, 32 категорії |

Понад узгоджений обсяг зроблено додатково:

| Що | Стан |
|---|---|
| PostgreSQL + Prisma замість файлів як джерела даних | ✅ |
| Заявки в БД + Telegram і e-mail | ✅ |
| GA4 зі згодою на cookie та подіями | ✅ |
| Картинки для соцмереж і фавікон | ✅ |
| Гео-посадкові: Дніпро, Київ, Харків, Запоріжжя | ✅ |
| RSS блогу обома мовами | ✅ |
| Docker, nginx, systemd, GitHub Actions | ✅ (образ не збирався, див. нижче) |
| 58 e2e-тестів у Chromium | ✅ |
| Мапа 301 з краулу старого сайту (982 URL, 159 правил) | ✅ |
| Розбір каталогів європейських і американських аналогів | ✅ |
| Секторні посадкові: кав'ярня, доставка, пекарня, фастфуд | ✅ |
| Інфраструктура A/B-тесту навігації + звіт в адмінці | ✅ (вимкнена, вмикається змінною) |

**Разом: 524 статичні сторінки, 134 товари, 32 категорії, 6 груп,
5 статей блогу, 4 гео-посадкові, 4 секторні посадкові.**

## 2. Ключові технічні рішення

1. **Два дерева маршрутів замість `[locale]` + rewrite.**
   Стандартний підхід із rewrite у proxy (Next 16 перейменував middleware.ts
   на proxy.ts) ламає клієнтську навігацію
   App Router: RSC-запити до переписаного шляху повертають 404. Виявлено
   браузерним тестом, гіпотезу про `trailingSlash` перевірено й відхилено.
   Тепер `src/app/(uk)` у корені та `src/app/(ru)/ru` над спільними
   реалізаціями в `src/views/`.

2. **301 у `proxy.ts`, а не в `next.config`.**
   При `trailingSlash: true` вбудований `redirects()` спрацьовує вже після
   нормалізації слеша й дає ланцюжок 308 → 301. Тому нормалізацію вимкнено
   (`skipTrailingSlashRedirect`) і слеш додає proxy — після перевірки
   legacy-мапи. Старий URL веде на новий **за один хоп**.

3. **Кошик поза React** (`useSyncExternalStore` + localStorage) — стан живе
   в браузерному сховищі, читати його ефектом означало б зайвий каскадний
   рендер і розсинхрон із SSR.

4. **Пошук через `/api/search/`**, а не імпортом каталогу в клієнт: інакше
   кожна сторінка тягнула б увесь `products.json` у бандл.

5. **Обидві ціни видно без реєстрації.** Роздрібна й усі оптові щаблі — прямо
   в картці товару. Це головна відмінність від конкурентів, які ховають опт
   за «зателефонуйте».

6. **Сумісність стакан ↔ кришка** зашита в дані за діаметром вінця
   (110–175 → 70 мм, 250 → 75, 340 → 80, 400–500 → 90). Знімає найчастішу
   помилку замовлення й працює як крос-продаж.

## 3. Результати QA

Прогнано в Chromium (desktop 1440×900 і mobile 390×844, світла й темна теми):

| Перевірка | Результат |
|---|---|
| Клієнтська навігація головна → каталог → категорія → товар | ✅ |
| Фільтри в категорії | ✅ 12 → 3 товари |
| Кошик: додавання, лічильник, збереження після перезавантаження | ✅ |
| Пошук за розміром «340» | ✅ 8 збігів |
| Пошук за артикулом і кирилицею в обох мовах | ✅ |
| Перемикач мови зберігає сторінку | ✅ |
| Помилки JS | немає |
| Помилки мережі (4xx/5xx) | немає |
| Горизонтальний скрол body | немає на жодній сторінці |
| 301 зі старих URL | 14/14 за один хоп → 200 |
| 404 на неіснуючій сторінці | ✅ |
| `title` ≤ 60 символів | ✅ на всіх перевірених |
| canonical + hreflang (uk/ru/x-default) | ✅ |
| `noindex` на кошику, оформленні, юридичних | ✅ |
| Schema.org валідність структури | Organization, LocalBusiness, Product, BreadcrumbList, FAQPage, Article |
| Рівно один `<h1>` на сторінці | ✅ на всіх 28 маршрутах × 2 мови |
| Усі поля форм мають підпис | ✅ |
| Наскрізне оформлення заявки | ✅ кошик → валідація → заявка → номер → очищення кошика |
| Калькулятор брендування | ✅ тираж і кольори змінюють ціну в правильний бік |
| Мобільна шторка фільтрів | ✅ Esc, пастка фокуса, блокування прокрутки, повернення фокуса |
| Мега-меню з клавіатури | ✅ Enter відкриває, Esc закриває |
| Skip-link першим у табуляції | ✅ |
| Видиме обведення фокуса | ✅ 2px |

Сумарно: **224 відкриття сторінок** (28 маршрутів × 2 мови × 2 пристрої ×
2 теми) без жодної помилки, плюс сценарні тести вище.

**Що знайшли й полагодили під час QA** — найцінніша частина цього етапу:

1. `/api/search` повертав порожній результат: `force-static` не передає
   `searchParams`. Пошук не працював узагалі.
2. `POST /api/lead` ішов через 308-редирект — тіло запиту могло губитись.
3. Клієнтська навігація не працювала: rewrite у proxy/middleware ламає RSC-запити
   App Router. Знайдено тільки браузерним тестом — curl показував 200.
4. Старі URL вели ланцюжком 308 → 301 замість одного хопу.
5. `NextURL` підганяв слеш цілі редиректу під вхідний запит, через що
   `/catalog/stakany-paperovi` редиректив сам на себе.
6. Мобільна шторка фільтрів не закривалась по Esc і не тримала фокус.
7. Кошик і тема читали `localStorage` в ефекті — зайвий каскадний рендер.
8. `proxy.ts` у корені проєкту Next **мовчки ігнорує**: після перейменування
   з `middleware.ts` (вимога Next 16) усі 301 перестали працювати, а збірка
   пройшла без жодного попередження. Файл має лежати в `src/`.
9. Сторінка 404 під `/ru/` показувалась українською, а перемикач мови на ній
   вів на неіснуючий `/ru/_not-found/`. Локаль тепер передається із proxy
   заголовком, який читає `global-not-found.tsx`.
10. Два екземпляри пошуку в шапці (десктопний і мобільний) мали однаковий
    `id` — ламалась прив'язка `<label>` до поля.

Кожен із цих десяти дефектів проходив повз `curl`, типізацію й лінт.
Знайшов їх лише прогін у справжньому браузері.

11. Пошук не знаходив «Стакан» з великої літери: `ILIKE` в PostgreSQL
    залежить від `LC_CTYPE` кластера, і при локалі `C` кирилиця не
    згортається взагалі. Тепер пошук іде по нормалізованому полю, яке
    будує застосунок, — цілий клас мовчазних поломок на чужому сервері
    прибрано.
12. У половини сторінок назва компанії дублювалась у заголовку:
    «Каталог … — SmartEcoPack | SmartEcoPack», бо шаблон кореневого
    layout додається після обрізки title.
13. `proxy` додавав завершальний слеш до `/icon` і `/og` — браузер
    отримував 301 замість картинки.
14. Картинка для соцмереж була відсутня на всіх сторінках, крім кореня:
    файлова конвенція `opengraph-image` чіпляється лише до кореня своєї
    групи маршрутів, а їх у проєкті дві.

### Постійні e2e-тести

Щоб знайдене не повернулось, перевірки зафіксовані як тести (`web/e2e/`,
Playwright, проти продакшн-збірки, desktop + mobile):

```bash
cd web && npm run test:e2e
```

58 тестів: клієнтська навігація без перезавантажень, фільтри, пошук,
наскрізне оформлення заявки, збереження кошика, калькулятор брендування,
мапа 301, sitemap, мета-теги й ліміт title, дублювання бренду, картинки
для соцмереж, RSS, гео-сторінки, 404 обома мовами, доступність шторки,
клавіатурна навігація, вхід в адмінку та поширення правки ціни на сайт,
секторні посадкові (canonical, обсяг тексту, посилання в каталог),
відсутність секторів у шапці й перемикання варіантів A/B.

Запуск у пісочниці, де Playwright не має власного Chromium:

```bash
PLAYWRIGHT_CHROMIUM_PATH=/opt/pw-browsers/chromium npm run test:e2e
```

### Продуктивність: чесна цифра

У дизайн-системі я заклав бюджет **< 120 KB JS gzip**. Фактично вийшло
**~180 KB**. Це майже повністю базовий рантайм React 19 + Next 16; власний
код сторінки — одиниці кілобайтів. Бюджет був нереалістичний для React-
застосунку, і я його не виконав. Що з цим робити:

- **Прийняти.** Сторінки віддаються статичним HTML, тому LCP від JS не
  залежить — контент видно до гідратації. Для B2B-каталогу це прийнятно.
- **Або скоротити** — прибрати інтерактивність із картки товару в сітці
  (степер і «в кошик» лишити тільки на сторінці товару). Це зменшить
  клієнтський код, але підніме кількість кліків до замовлення. Не рекомендую.

Реальні Core Web Vitals треба міряти після запуску на бойовому хостингу —
локальні цифри не показові.

## 4. Чек-лист запуску

### Блокери — без цього запускати не можна

| # | Блокер | Стан |
|---|---|---|
| 1 | **Мапа 301** | ✅ **Знято краулом від 02.04.2025.** 159 правил, усі 330 старих URL (крім товарів) перевірені автоматично й ведуть на сторінку, яка існує |
| 2 | **Реальні товари** | ⛔ Відкрито. 299 позицій на старому сайті проти 134 демонстраційних у нас. Потрібне вивантаження з адмінки: артикул, назва, категорія, фасування, ціна, наявність |
| 3 | **Слаги товарів** | ⛔ Відкрито. Рекомендація: **зберегти старі слаги** — тоді `/products/:slug → /product/:slug/` покриває всі 299 позицій без окремої мапи |
| 4 | **Фото товарів** | ⛔ Відкрито. Поки показуємо силует за формою товару |
| 5 | Реальні контакти Telegram/Viber | ⛔ Зараз плейсхолдери в `src/lib/site.ts` |
| 6 | Доступи: хостинг, DNS, Search Console | ⛔ Потрібні для перемикання й моніторингу |

### Перед відкриттям індексації

- [ ] Фото товарів; замінити `Placeholder` на `next/image`
- [ ] Перевірити мапу 301 на повному переліку URL зі старого сайту
- [ ] Google Search Console: підтвердити домен, залити sitemap
- [ ] Google Analytics 4 + події (заявка, дзвінок, клік у месенджер, кошик)
- [ ] Google Business Profile: звірити NAP із `src/lib/site.ts`
- [ ] Перевірити розмітку в Rich Results Test
- [ ] Реальні контакти Telegram/Viber у `src/lib/site.ts` (зараз плейсхолдери)
- [ ] OG-зображення `public/og.png` (на нього вже посилається розмітка)
- [ ] favicon під бренд

### Після запуску

- [ ] Тиждень 1: щоденно дивитись 404 у GSC — ловити дірки в мапі 301
- [ ] Коли трафік стабілізується — увімкнути A/B-тест навігації:
      `NEXT_PUBLIC_AB_NAV=1` у `.env`, перезбірка, звіт у `/admin/ab/`.
      Не дивитись результат раніше 1500 сесій на варіант і двох повних тижнів
- [ ] Тиждень 1–2: моніторити позиції за топ-30 запитами
- [ ] Місяць 1: порівняти органіку зі старим сайтом
- [ ] Заміряти Core Web Vitals на бойовому хостингу

## 5. Що свідомо не робилось

| Що | Чому |
|---|---|
| Онлайн-оплата | За рішенням: каталог + кошик-заявка, менеджер виставляє рахунок |
| Особистий кабінет клієнта | Немає в узгодженому обсязі; історія замовлень доступна менеджеру в адмінці |
| Розгорнуті тексти решти категорій | Топ-8 (≈70 % трафіку) розгорнуті до 1200–2800 знаків із підзаголовками. Решта має по 1–2 абзаци — їх можна дописати прямо в адмінці, розробник для цього не потрібен |
| Справжній A/B-тест зараз | Немає трафіку. Щоб побачити різницю 15 % при базовій конверсії 3 %, потрібно 1500–2000 сесій на варіант, тобто 2–4 тижні. Побудована інфраструктура; будь-яка цифра раніше — шум, що виглядає як результат |
| Фото товарів | Немає вихідних матеріалів; поки показуємо силует за формою товару |
| Автоматичний деплой із CI | CI перевіряє, але не розкочує: спершу треба вирішити, куди саме (VPS чи хмара) |

## 6. Що потрібно від вас

### Отримано

| Файл | Що дав |
|---|---|
| ✅ **Краул від 02.04.2025** (982 URL) | Мапу 301: 159 правил, 34 категорії, 18 статей, усі службові сторінки. Блокер №1 знято |

### Ще потрібно

| # | Що | Джерело | Без цього не можна |
|---|---|---|---|
| 1 | **`products.csv`** — 299 позицій: артикул, назва uk/ru, категорія, фасування, ціна роздрібна й оптова, наявність | вивантаження зі старої адмінки; колонки описані на сторінці «Імпорт» в адмінці | Запускати каталог. Зараз у нас 134 демонстраційні товари з вигаданими цінами |
| 2 | **Рішення по слагах товарів** | ваша відповідь: лишаємо старі чи робимо нові | Мапу 301 для товарів. Рекомендую лишити старі — тоді мапа не потрібна взагалі |
| 3 | **Фото товарів** в оригінальній якості | ваш архів або фотозйомка | Картки виглядають порожньо: зараз силует замість фото |
| 4 | `gsc-queries.csv` — експорт за 12 місяців | Search Console → Ефективність → Експорт | Пріоритет наповнення текстами. Зараз він побудований на моїй оцінці обсягів, а не на ваших реальних запитах |
| 5 | Умови: мінімальна партія, поріг опту, тиражі й ціни брендування | ви | Цифри на сайті зараз мої припущення |
| 6 | Реальні Telegram і Viber, токен Telegram-бота | ви | Заявки не доходитимуть до менеджера |
| 7 | Логотип у векторі | ваш дизайнер | Зараз логотип відмальований мною зі скріншотів |
| 8 | Доступи: домен, хостинг, GA4, Search Console | ви | Перемикання й моніторинг після запуску |

### Що з цього критично

Пункти **1–3** блокують запуск. Решта — псує якість, але не зупиняє.

Після отримання `products.csv` до запуску лишається 2–3 робочі дні: імпорт даних,
підключення каналу заявок, фінальний QA.
