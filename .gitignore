# Залежності
node_modules/

# База даних (створюється автоматично при першому запуску)
data/*.db
data/*.db-shm
data/*.db-wal

# Завантажені файли договорів (зберігаються разом із базою, у git не потрапляють)
data/uploads/

# Локальні налаштування
.env
.env.local

# Логи
*.log
npm-debug.log*

# ОС / редактор
.DS_Store
Thumbs.db
.vscode/
.idea/
