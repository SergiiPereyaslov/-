# 🧾 Веб-система виставлення рахунків

Веб-інтерфейс для виставлення рахунків та видаткових накладних замість Excel.
Створений для **ТОВ без ПДВ**, розрахований на **кількох менеджерів** зі спільною
базою. Інтерфейс і документи — українською.

## Можливості

- **Рахунок + видаткова накладна** з однаковими даними та номером.
- **Друк комплекту** однією кнопкою: **2 рахунки + 2 видаткові накладні**, по два
  документи формату **А5 на одному аркуші А4** (аркуш можна розрізати навпіл).
- **Стандартний український вигляд** документів; сума прописом українською з
  правильними родовими формами.
- **База замовників** (уже завантажено 238) із **пошуком за повною назвою, короткою
  назвою та кодом**, сортуванням і пагінацією. У формі рахунку замовник обирається
  через автодоповнення. У назвах застосовано узгоджені скорочення (ВСП, ДПТНЗ, КЗВО тощо).
- **Каталог товарів** (уже завантажено перелік продукції) — швидке додавання
  позицій; **ціну менеджер проставляє вручну** для кожного рахунку (різні регіони).
- **Кілька банківських рахунків** — при виставленні рахунку менеджер обирає, на
  який рахунок платити; один можна позначити типовим.
- **Дату можна не вказувати** — для бюджетних установ, які платять не день-у-день;
  у документі тоді залишається місце під дату для заповнення від руки.
- **Папка замовника**: реквізити, договір, **прикріплені файли** (Word, PDF, скани —
  завантажуються й зберігаються поруч із замовником) та історія документів із
  сортуванням **спочатку нові / спочатку старі** й кнопкою **«Копія»**.
- **Історія рахунків** із пошуком, фільтрами та статусами оплати.
- **Автоматична нумерація**, **логін менеджерів**, облік автора, керування
  користувачами, копіювання рахунку.

## Початкові дані

При першому запуску (якщо база порожня) автоматично завантажуються:
- **замовники** з `seed/institutions.json` (розділ «Замовники»);
- **товари** з `seed/products.json` (розділ «Товари та послуги»), усі з ціною 0.

Дані завантажуються лише один раз і не перезаписують ваші зміни.

### Долити нових замовників у наповнену базу

Перелік замовників живий: з часом з'являються нові. Щоб додати їх до бази,
у якій уже є дані, впишіть їх у `seed/institutions.json` і виконайте:

```bash
npm run import:institutions               # додасть лише тих, яких ще немає
npm run import:institutions -- --dry      # лише показати, нічого не змінювати
npm run import:institutions -- --replace  # видалити наявних і залити заново
```

`--replace` не торкається замовників, по яких уже є рахунки: інакше історія
документів втратила б зв'язок із замовником. Видалення й заливання виконуються
однією транзакцією, тож збій не залишить базу напівпорожньою.

Команда порівнює замовників за кодом і за назвою (без огляду на лапки, апострофи
та регістр), тож дублів не створює — її можна запускати повторно.

## Вимоги

- **Node.js 22.5+** (використовується вбудований `node:sqlite`).
- База — файл SQLite, окремий сервер БД не потрібен.

## Встановлення та запуск

```bash
npm install
npm start
```

Відкрийте **http://localhost:3000**. Обліковий запис за замовчуванням:
**логін `admin`, пароль `admin`** (одразу змініть пароль і заповніть «Реквізити»).

Порт змінюється через змінну оточення: `PORT=8080 npm start`.

## Перші кроки

1. Увійдіть як `admin` / `admin` і **змініть пароль**.
2. У «Реквізити» заповніть дані ТОВ і **додайте банківські рахунки**.
3. У «Користувачі» додайте менеджерів.
4. Замовники й товари вже завантажені — за потреби додавайте/редагуйте їх.
5. «Новий рахунок» → оберіть замовника (пошуком), рахунок для оплати, додайте позиції
   й ціни, за потреби зніміть дату → **«Друк комплекту»** (2 рахунки + 2 накладні).

## Розгортання для кількох менеджерів

Запустіть застосунок на спільному комп'ютері/сервері в мережі офісу або на VPS;
менеджери заходять у браузері за його адресою. Дані — у файлі `data/invoices.db`
(режим WAL для одночасної роботи).

**Windows/macOS не має значення для менеджерів** — після розгортання на
спільному сервері вони просто відкривають адресу в браузері (Chrome, Edge,
Safari — будь-який), незалежно від того, яка ОС на їхньому комп'ютері.
Сам застосунок (Node.js), LibreOffice і Document Server теж однаково
працюють і на macOS, і на Windows, і на Linux — ОС сервера обирає
IT-фахівець. Команди нижче з `bash` і `PowerShell` наведені паралельно
там, де синтаксис відрізняється (в основному — для локального тестування).

### Доступ з будь-якої точки світу (для IT-фахівця)

Щоб менеджери заходили не лише з офісної мережі, а й ззовні, застосунок має
працювати на сервері з постійним доступом до інтернету (VPS чи хмарна
платформа), а не на робочому комп'ютері.

**1. Вимоги.** Node.js 22.5+, порт назовні (за замовчуванням 3000, змінюється
через `PORT`). Окрема СУБД не потрібна — база SQLite зберігається у файлі.

**2. Запуск застосунку як постійного сервісу**, щоб він автоматично
перезапускався після падіння чи перезавантаження сервера. Найпростіше —
[pm2](https://pm2.keymetrics.io/):

```bash
npm install -g pm2
pm2 start server.js --name invoice-web
pm2 save
pm2 startup   # виводить команду для автозапуску після перезавантаження сервера
```

Так само підійде звичайний `systemd`-юніт, якщо у вас так прийнято.

**3. HTTPS обов'язковий**, оскільки логін і пароль передаватимуться через
відкритий інтернет. Найпростіше — зворотний проксі (nginx або
[Caddy](https://caddyserver.com/), який сам випускає й оновлює сертифікат
Let's Encrypt), що приймає з'єднання на 443 порту й перенаправляє на
`localhost:3000`, де працює застосунок.

Після налаштування HTTPS увімкніть у застосунку два прапорці — обидва
вимкнені за замовчуванням, щоб не заважати локальному тестуванню без HTTPS:

```bash
TRUST_PROXY=1 COOKIE_SECURE=1 PORT=3000 pm2 start server.js --name invoice-web
```

- `TRUST_PROXY=1` — щоб Express правильно розпізнавав з'єднання через проксі.
- `COOKIE_SECURE=1` — щоб сесія передавалася лише зашифрованим з'єднанням.

**4. Домен** — необов'язковий, але зручний (напр. `rahunky.odcpit.com` замість
IP-адреси); можна підключити пізніше без переналаштування самого застосунку.

**5. Резервне копіювання каталогу `data/`** (див. нижче) стає ще важливішим
на віддаленому сервері — налаштуйте автоматичне копіювання (напр. `cron` +
`rsync`/`rclone` у хмарне сховище).

**6. Перше, що варто зробити після розгортання** — увійти як `admin` / `admin`
і одразу змінити пароль у розділі «Користувачі».

## Резервне копіювання

Копіюйте каталог **`data/`** — там і база (`invoices.db*`), і завантажені файли
договорів (`data/uploads/`). Відновлення — повернути каталог на місце.

## Тести

```bash
npm test
```

## Структура проєкту

```
server.js               # запуск, автентифікація, дашборд, маршрути
src/
  db.js                 # база (node:sqlite), схема, міграції, seed
  auth.js               # логін, сесії, права доступу
  format.js             # сума прописом, формат грошей і дат
  invoiceHelpers.js     # нумерація, розбір і підрахунок позицій
  routes/               # invoices, clients, services, settings
views/                  # шаблони EJS; друковані документи та комплект
public/                 # CSS та JS (стилі друку: print.css, print-set.css)
seed/                   # початкові дані: institutions.json, products.json
data/                   # база даних SQLite (створюється автоматично)
test/                   # автотести
```

### Перегляд файлів договорів у браузері

У картці замовника кожен прикріплений файл можна не лише завантажити, а й
**переглянути прямо в браузері** («Переглянути»), не зберігаючи його на
комп'ютер. PDF і зображення (.jpg, .png) браузер показує сам. Word/Excel/
RTF/ODT/TXT (.doc, .docx, .xls, .xlsx, .rtf, .odt, .txt) конвертуються в
PDF на льоту через [LibreOffice](https://www.libreoffice.org/) — його треба
встановити на комп'ютері, де запущено сервер:

```bash
# macOS
brew install --cask libreoffice
# Ubuntu/Debian (для сервера)
sudo apt install libreoffice
```

```powershell
# Windows (PowerShell) — можна й просто встановити .exe із libreoffice.org
winget install --id TheDocumentFoundation.LibreOffice -e
```

Якщо LibreOffice не встановлено, кнопка «Переглянути» для таких файлів
покаже зрозуміле повідомлення й запропонує звичайне завантаження — нічого
не ламається. Перша конвертація файлу займає кілька секунд, результат
кешується, тож повторний перегляд миттєвий.

### Редагування договорів прямо в сервісі (OnlyOffice)

Кнопка «Редагувати» (поруч із «Переглянути»/«Друк», для Word/Excel/RTF/ODT/
TXT-файлів) відкриває повноцінний редактор прямо в браузері й зберігає
зміни назад у той самий файл — без завантаження на комп'ютер. Це працює
через [OnlyOffice Document Server](https://www.onlyoffice.com/) — окремий
безкоштовний сервіс, який потрібно один раз запустити поруч із застосунком
(зазвичай через Docker). Без нього кнопка просто не показується, решта
сервісу (перегляд, друк, завантаження) працює як завжди.

**1. Встановіть Docker Desktop**, якщо ще не встановлено —
[docker.com/products/docker-desktop](https://www.docker.com/products/docker-desktop/)
(або `brew install --cask docker` на macOS). Запустіть програму й дочекайтесь,
поки вона повністю завантажиться (значок кита в меню-барі стане активним).

**2. Запустіть Document Server** одноразовою командою в Терміналі:

```bash
docker run -d -p 8082:80 --name onlyoffice-ds --restart unless-stopped onlyoffice/documentserver
```

Перевірте, що спрацювало: відкрийте http://localhost:8082 — має з'явитися
вітальна сторінка OnlyOffice. Контейнер лишається запущеним і після
перезавантаження комп'ютера (`--restart unless-stopped`); зупинити:
`docker stop onlyoffice-ds`, запустити знову: `docker start onlyoffice-ds`.

**3. Запустіть застосунок з двома змінними оточення:**

```bash
# macOS/Linux (bash/zsh)
ONLYOFFICE_URL=http://localhost:8082 \
ONLYOFFICE_CALLBACK_URL=http://host.docker.internal:3000 \
npm start
```

```powershell
# Windows (PowerShell)
$env:ONLYOFFICE_URL="http://localhost:8082"
$env:ONLYOFFICE_CALLBACK_URL="http://host.docker.internal:3000"
npm start
```

`host.docker.internal` працює однаково на Docker Desktop для Mac, Windows
і Linux — саме так контейнер звертається до застосунку, що працює поза
контейнером, незалежно від ОС.

- `ONLYOFFICE_URL` — адреса Document Server, яку відкриває **браузер**
  менеджера (щоб завантажити сам редактор).
- `ONLYOFFICE_CALLBACK_URL` — адреса, за якою **сам Document Server**
  (він працює всередині контейнера) звертається до нашого застосунку за
  файлом і зберігає результат. Це обов'язково `host.docker.internal`, а
  не `localhost` — усередині контейнера `localhost` означає сам контейнер,
  а не комп'ютер, на якому він запущений. Якщо забути цю змінну — редактор
  відкриється, але при збереженні зміни не долетять.

**4. (Необов'язково, для продакшену) Захист JWT.** Без додаткових
налаштувань зв'язок між застосунком і Document Server не підписаний —
прийнятно для локального тесту на одному комп'ютері, але для розгортання
назовні варто увімкнути перевірку. Придумайте довгий випадковий рядок і
передайте його в обидва місця:

```bash
# при запуску контейнера (додатково до команди з кроку 2):
-e JWT_ENABLED=true -e JWT_SECRET=довгий-випадковий-секрет

# при запуску застосунку (додатково до змінних з кроку 3):
ONLYOFFICE_JWT_SECRET=довгий-випадковий-секрет
```

Підтримувані формати: Word (.doc, .docx, .odt, .rtf, .txt) і Excel
(.xls, .xlsx). PDF і зображення так само лишаються лише для перегляду —
для них є «Переглянути»/«Друк», а не «Редагувати».

### Прикріпити договори з каталогу на диску

Якщо договори лежать у папках, названих за замовником (по папці на замовника),
їх можна прикріпити однією командою — запускати на тому комп'ютері, де лежать
файли:

```bash
npm run import:contracts -- "F:\2026\Автоматизація ОДЦПІТ"
npm run import:contracts -- "F:\..." --dry   # показати, нічого не змінювати
```

Назва папки зіставляється з назвою замовника з урахуванням скорочень, лапок і
регістру. Повторний запуск не дублює: файл із такою самою назвою й розміром
пропускається. Для папок, яким не знайшлося замовника, команда виводить список —
створіть їх у розділі «Замовники» і запустіть ще раз.

### Скорочення в назвах замовників

Довгі формулювання скорочуються за узгодженою розкладкою (`src/abbreviate.js`):

| Повна форма | Скорочення |
|---|---|
| Відокремлений структурний підрозділ Фаховий коледж | ВСПФК |
| Державний професійно-технічний навчальний заклад | ДПТНЗ |
| Вищий навчальний приватний заклад | ВНПЗ |
| Відокремлений структурний підрозділ | ВСП |
| Комунальний вищий навчальний заклад | КВНЗ |
| Державний вищий навчальний заклад | ДВНЗ |
| Комунальний заклад вищої освіти | КЗВО |
| Комунальний заклад освіти | КЗО |
| Державний навчальний заклад | ДНЗ |
| Відокремлений підрозділ | ВП |
| Вище професійне училище | ВПУ |
| Комунальний заклад | КЗ |

Довші формулювання скорочуються першими, тож «Комунальний заклад вищої освіти»
дає КЗВО, а не «КЗ вищої освіти».

## Технології

Node.js · Express · EJS · вбудований SQLite (`node:sqlite`) · bcrypt.
Без важких залежностей і без етапу збірки.
