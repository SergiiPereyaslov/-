<%- include('../partials/head', { title }) %>
<%- include('../partials/nav') %>

<div class="page-head">
  <h1><%= title %></h1>
  <a href="/clients" class="btn btn-ghost">← До списку</a>
</div>

<% if (typeof error !== 'undefined' && error) { %>
  <div class="alert alert-error"><%= error %></div>
<% } %>

<form method="post" action="<%= action %>" class="card form-narrow">
  <label class="block">
    <span>Назва закладу <span class="req">*</span></span>
    <input type="text" name="name" value="<%= client.name || '' %>" required autofocus placeholder="Повна назва навчального закладу">
  </label>

  <div class="form-grid-2">
    <label>
      <span>Код</span>
      <input type="text" name="code" value="<%= client.code || '' %>" placeholder="Напр.: 2799">
    </label>
    <label>
      <span>Коротка назва</span>
      <input type="text" name="short_name" value="<%= client.short_name || '' %>" placeholder="Напр.: ДНУ">
    </label>
  </div>

  <label class="block">
    <span>Категорія</span>
    <input type="text" name="category" value="<%= client.category || '' %>" list="catList" placeholder="Напр.: Заклад вищої освіти">
    <datalist id="catList">
      <option value="Заклад вищої освіти"></option>
      <option value="Заклади фахової передвищої освіти"></option>
    </datalist>
  </label>

  <details class="more-fields">
    <summary>Додаткові реквізити (необов’язково)</summary>
    <div class="more-body">
      <div class="form-grid-2">
        <label>
          <span>Код ЄДРПОУ</span>
          <input type="text" name="edrpou" value="<%= client.edrpou || '' %>">
        </label>
        <label>
          <span>Контактна особа</span>
          <input type="text" name="contact_person" value="<%= client.contact_person || '' %>">
        </label>
      </div>
      <label class="block">
        <span>Адреса</span>
        <input type="text" name="address" value="<%= client.address || '' %>">
      </label>
      <div class="form-grid-2">
        <label>
          <span>Телефон</span>
          <input type="text" name="phone" value="<%= client.phone || '' %>">
        </label>
        <label>
          <span>Email</span>
          <input type="text" name="email" value="<%= client.email || '' %>">
        </label>
      </div>
    </div>
  </details>

  <div class="form-actions">
    <button type="submit" class="btn btn-primary">Зберегти</button>
    <a href="/clients" class="btn btn-ghost">Скасувати</a>
  </div>
</form>

<%- include('../partials/foot') %>
