name: CI

on:
  push:
    branches: ['**']
  pull_request:

# Нова збірка скасовує попередню для тієї ж гілки — немає сенсу тримати
# чергу з застарілих прогонів
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  web:
    name: Перевірки сайту
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: sep
          POSTGRES_PASSWORD: sep
          POSTGRES_DB: smartecopack
          # UTF-8 обов'язково: при локалі C PostgreSQL не згортає кирилицю
          POSTGRES_INITDB_ARGS: '--locale=C.UTF-8 --encoding=UTF8'
        ports: ['5432:5432']
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql://sep:sep@localhost:5432/smartecopack
      NEXT_PUBLIC_SITE_URL: http://127.0.0.1:3300
      NEXT_TELEMETRY_DISABLED: '1'

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Встановити залежності
        run: npm ci

      - name: Міграції та сідування
        run: |
          npx prisma migrate deploy
          npm run db:seed

      - name: Перевірка типів
        run: npm run typecheck

      - name: Лінт
        run: npm run lint

      # Правила скорочення заголовків: тести чисті, БД і збірка не потрібні
      - name: Модульні тести
        run: npm run test:unit

      # Обсяг текстів каталогу читається з БД, тому йде після сідування
      - name: Обсяг текстів каталогу
        run: npm run check:content

      - name: Збірка
        run: npm run build

      - name: Браузер для e2e
        run: npx playwright install --with-deps chromium

      - name: E2E
        run: npm run test:e2e

      # Суцільна перевірка мета-тегів усіх сторінок плюс прогін формули
      # заголовка на 299 реальних назвах зі старого сайту. Потрібен
      # запущений сайт, тому крок іде після e2e.
      - name: Мета-теги
        run: |
          npx next start -p 3400 &
          # Чекаємо готовності без зайвої залежності: npx wait-on тягнув би
          # пакет із мережі на кожному прогоні заради одного циклу
          for i in $(seq 1 60); do
            curl -sf -o /dev/null http://127.0.0.1:3400/ && break
            sleep 2
          done
          npm run check:meta
        env:
          CHECK_BASE: http://127.0.0.1:3400

      - name: Звіт e2e при падінні
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: web/playwright-report/
          retention-days: 7
